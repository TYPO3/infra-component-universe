name: Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install
        run: |
          yarn install
      - name: Build
        run: |
          yarn build
          
  deploy:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - name: Status
        run: ls -lah
